#pragma once

#include "simd.h"
#include "types.h"

#if COMPILER_MSVC
#include <intrin.h>
#endif

struct FlArena;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

typedef struct Vec2 {
    float x;
    float y;
} Vec2;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

typedef struct Ivec2 {
    int x;
    int y;
} Ivec2;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline u64 align_pow2(u64 value, u64 alignment) {
    return (value + alignment - 1) & ~(alignment - 1);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Bit manipulation functions

static inline u32 clz_u64(u64 value) {
#if COMPILER_CLANG || COMPILER_GCC
    return value ? __builtin_clzll(value) : 64;
#elif COMPILER_MSVC
    unsigned long index;
    return _BitScanReverse64(&index, value) ? (63 - index) : 64;
#else
#error "Unsupported compiler"
#endif
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline int count_trailing_zeros(unsigned int value) {
#if defined(__GNUC__) || defined(__clang__)
    return __builtin_ctz(value);
#elif defined(_MSC_VER)
    unsigned long index;
    _BitScanForward(&index, value);
    return (int)index;
#else
#error "Unsupported compiler"
#endif
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

typedef struct IntersectResult {
    u32* indices;
    int count;
} IntersectResult;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

IntersectResult f16x8_aabb_intersect_f32x4_fixed_array(struct FlArena* arena, const f16x8_soa_array* aabb_array,
                                                       f32x4 test_aabb);

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline f32 min_f32_impl(f32 a, f32 b) {
    return a < b ? a : b;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline f32 max_f32_impl(f32 a, f32 b) {
    return a > b ? a : b;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline i32 min_i32_impl(i32 a, i32 b) {
    return a < b ? a : b;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline i32 max_i32_impl(i32 a, i32 b) {
    return a > b ? a : b;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline f32 clamp_f32_impl(f32 v, f32 min_val, f32 max_val) {
    if (v < min_val)
        return min_val;
    if (v > max_val)
        return max_val;
    return v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline f32 abs_f32_impl(f32 v) {
    return v < 0.0f ? -v : v;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline f32 lerp_f32_impl(f32 a, f32 b, f32 t) {
    return a + (b - a) * t;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline f32 inv_lerp_f32_impl(f32 a, f32 b, f32 v) {
    return (v - a) / (b - a);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static inline f32 saturate_f32_impl(f32 v) {
    return clamp_f32_impl(v, 0.0f, 1.0f);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Short spellings for the f32 implementation functions

#define lerp(a, b, t) lerp_f32_impl(a, b, t)

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#define inv_lerp(a, b, v) inv_lerp_f32_impl(a, b, v)

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#define saturate(v) saturate_f32_impl(v)

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#define abs_f32(v) abs_f32_impl(v)
